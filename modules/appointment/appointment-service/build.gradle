dependencies {
	compile project(":modules:appointment:appointment-api")

	compileOnly group: "com.liferay.portal", name: "release.dxp.api"
}

buildService {
	apiDir = "../appointment-api/src/main/java"
}

group = "com.liferay.hms.appointment"